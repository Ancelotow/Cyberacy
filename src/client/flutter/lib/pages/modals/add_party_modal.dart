import 'package:flutter/material.dart';

class AddPartyModal extends StatelessWidget {
  const AddPartyModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AlertDialog(

    );
  }
}
